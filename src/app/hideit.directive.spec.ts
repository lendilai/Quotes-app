import { HideitDirective } from './hideit.directive';

describe('HideitDirective', () => {
  it('should create an instance', () => {
    const directive = new HideitDirective();
    expect(directive).toBeTruthy();
  });
});
